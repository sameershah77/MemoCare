<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SilentEcho" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/grey1</item>
    </style>

    <style name="Theme.SilentEcho" parent="Base.Theme.SilentEcho" />
</resources>